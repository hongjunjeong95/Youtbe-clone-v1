import React, { memo } from "react";
import Header from "../components/header";

const Home = memo(() => {
  return (
    <div>
      <Header></Header>
    </div>
  );
});

export default Home;
